{
  "//": "Every place a version lives. scripts/release.mjs is the only thing that writes them.",
  "files": [
    {
      "path": "plugin.json",
      "field": "version"
    },
    {
      "path": ".claude-plugin/plugin.json",
      "field": "version"
    },
    {
      "path": ".codex-plugin/plugin.json",
      "field": "version"
    },
    {
      "path": "package.json",
      "field": "version"
    },
    {
      "path": ".claude-plugin/marketplace.json",
      "field": "plugins.*.version",
      "//": "release.mjs keeps only the core plugin entry in lockstep; sector plugin versions stay independent."
    }
  ],
  "//skills": "Frontmatter version in every capability skill. Sector skills (metadata.countries) are skipped by release.mjs so a vertical can ship on its own version.",
  "skills": {
    "glob": "skills/*/SKILL.md",
    "field": "metadata.version"
  },
  "audit": {
    "//": "--audit greps the repo for the previous version string. Anything matched here is expected to carry it.",
    "exclude": [
      ".git",
      ".version-bump.json",
      "CHANGELOG.md",
      "node_modules",
      "package-lock.json",
      "scripts/release.mjs"
    ]
  }
}
